---
// BaseLayout.astro
import '../styles/global.css';

interface Props {
  title: string;
  description: string;
  og_title?: string;
  og_description?: string;
  image?: string;
  type?: string;
}
const {
  title,
  description,
  og_title,
  og_description,
  image = "/images/2025-01-Ochki-zerkalnie-sky.jpg",
  type = "website",
} = Astro.props;

const BASE = "https://volksunglasses.com";
const ogImage = image.startsWith("http") ? image : `${BASE}${image}`;
const ogTitle = og_title ?? title.substring(0, 60);
const ogDesc = og_description ?? description.substring(0, 200);

const canonical = Astro.url.href;
const siteName = "VOLK Sunglasses";
---

<!doctype html>
<html lang="ru" dir="ltr">
  <head>
    <meta charset="UTF-8" />
    <!-- Hero image preload — must be first to minimize LCP resource delay -->
    <link rel="preload" as="image" href="/hero-volk.webp" type="image/webp" fetchpriority="high" />
    <link rel="preload" as="image" href="/hero-volk.jpg" fetchpriority="high" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, minimum-scale=1.0, maximum-scale=5.0, viewport-fit=cover" />
    <meta name="theme-color" content="#080808" />
    <meta name="robots" content="index, follow, max-image-preview:large, max-snippet:-1, max-video-preview:-1" />

    <!-- Primary SEO -->
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={canonical} />
    <link rel="alternate" hreflang="ru-RU" href={canonical} />
    <link rel="alternate" hreflang="x-default" href={canonical} />

    <!-- Open Graph -->
    <meta property="og:type" content={type} />
    <meta property="og:site_name" content={siteName} />
    <meta property="og:title" content={ogTitle} />
    <meta property="og:description" content={ogDesc} />
    <meta property="og:image" content={ogImage} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:url" content={canonical} />
    <meta property="og:locale" content="ru_RU" />

    <!-- Twitter -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:site" content="@volksunglasses" />
    <meta name="twitter:creator" content="@volksunglasses" />
    <meta name="twitter:title" content={ogTitle} />
    <meta name="twitter:description" content={ogDesc} />
    <meta name="twitter:image" content={ogImage} />

    <!-- Geo (Russia) -->
    <meta name="geo.region" content="RU" />
    <meta name="language" content="ru" />

    <!-- Organization JSON-LD -->
    <script type="application/ld+json">
      {
        "@context": "https://schema.org",
        "@graph": [
          {
            "@type": "Organization",
            "@id": "https://volksunglasses.com/#organization",
            "name": "VOLK Sunglasses",
            "url": "https://volksunglasses.com",
            "logo": {
              "@type": "ImageObject",
              "url": "https://volksunglasses.com/images/logo-volk.png",
              "width": 100,
              "height": 59
            },
            "foundingDate": "2019",
            "foundingLocation": {
              "@type": "Place",
              "name": "San Petersburgo, Rusia"
            },
            "contactPoint": {
              "@type": "ContactPoint",
              "telephone": "+7-960-273-70-38",
              "contactType": "customer service",
              "availableLanguage": "Russian"
            },
            "sameAs": [
              "https://www.wildberries.ru/brands/volk-sunglasses",
              "https://www.instagram.com/volk.sunglasses/",
              "https://vk.com/volksunglasses",
              "https://t.me/volksunglasses"
            ]
          },
          {
            "@type": "WebSite",
            "@id": "https://volksunglasses.com/#website",
            "url": "https://volksunglasses.com",
            "name": "VOLK Sunglasses",
            "publisher": { "@id": "https://volksunglasses.com/#organization" },
            "potentialAction": {
              "@type": "SearchAction",
              "target": "https://volksunglasses.com/katalog?q={search_term_string}",
              "query-input": "required name=search_term_string"
            }
          }
        ]
      }
    </script>

    <!-- Fonts: async non-blocking -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      rel="stylesheet"
      href="https://fonts.googleapis.com/css2?family=Oswald:wght@600;700&family=Inter:wght@300;400;500;600;700&family=Bebas+Neue&display=swap"
      media="print"
      onload="this.media='all'"
    />
    <noscript>
      <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Oswald:wght@600;700&family=Inter:wght@300;400;500;600;700&family=Bebas+Neue&display=swap" />
    </noscript>

    <!-- Hero image preload (critical LCP resource) -->
    <link rel="preload" as="image" href="/hero-volk.webp" type="image/webp" fetchpriority="high" />
    <link rel="preload" as="image" href="/hero-volk.jpg" fetchpriority="high" />

    <!-- Favicon -->
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />
  </head>
  <body>
    <slot />
    <!-- Google Tag Manager — deferred after load for better LCP/INP -->
    <noscript><iframe src="https://www.googletagmanager.com/ns.html?id=GTM-TXNHBWQ"
    height="0" width="0" style="display:none;visibility:hidden"></iframe></noscript>
    <script is:inline>
      (function() {
        var loadGTM = function() {
          (function(w,d,s,l,i){w[l]=w[l]||[];w[l].push({'gtm.start':
          new Date().getTime(),event:'gtm.js'});var f=d.getElementsByTagName(s)[0],
          j=d.createElement(s),dl=l!='dataLayer'?'&l='+l:'';j.async=true;j.src=
          'https://www.googletagmanager.com/gtm.js?id='+i+dl;f.parentNode.insertBefore(j,f);
          })(window,document,'script','dataLayer','GTM-TXNHBWQ');
        };
        if ('requestIdleCallback' in window) {
          requestIdleCallback(loadGTM, { timeout: 4000 });
        } else {
          window.addEventListener('load', function() { setTimeout(loadGTM, 300); });
        }
      })();
    </script>
  </body>
</html>
